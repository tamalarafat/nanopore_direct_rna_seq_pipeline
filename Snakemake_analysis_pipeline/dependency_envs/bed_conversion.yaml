channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - r-base=4.4.1  # r-base
  - r-stringr     # stringr package for the function
  - r-readr       # for reading and writing data
  - r-data.table  # for data manipulation
  - pip           # optional use
  - r-yaml        # read configuration file